// ltl-lz77/main.ltl
//
// LZ77 sliding-window compressor (educational).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-lz77 v0.1.0")
    io::println("LZ77 sliding-window compressor (educational).")
    0
}
