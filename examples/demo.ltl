// examples/demo.ltl — runnable demo for ltl_lz77

import std::io
use ltl_lz77 as lib

fn main() {
    io::println("ltl_lz77 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}